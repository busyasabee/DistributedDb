# distributedbHibernate
Master-slave replication using Java, MySQL, JDBC, Hibernate. Master is localhost, slave - virtual machine VirtualBox. Write operations go to the master, readings to the slave.
